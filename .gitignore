.DS_Store
.gradle
.idea
.intellijPlatform
.kotlin
.qodana
build
# Generated by `npm run build` in ui/ (vite outDir, emptyOutDir wipes it each build)
src/main/resources/player/

# Local-only test media: large royalty-free tracks for manual inspector testing.
# assets/ is symlinked into ui/public and bundled into the jar, so keeping these
# out of git keeps them out of git history and the shipped plugin. (sintel.* stay.)
/assets/*.mp3
!/assets/sintel.mp3
